# Example environment variables for the client.
# Copy this to `.env` locally and fill with values.
# DO NOT commit the `.env` file.

# Server API base URL (leave empty for localhost fallback)
VITE_API_BASE=http://localhost:5000

# For production, set this to your deployed server URL:
# VITE_API_BASE=https://your-backend-url.vercel.app
